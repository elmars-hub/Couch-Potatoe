export interface Favorite {
  movie_id: string;
  created_at: string;
  // Add more fields if needed
}

export interface Bookmark {
  movie_id: string;
  created_at: string;
  // Add more fields if needed
}
